package main

import "fmt"

var c, python, cplus bool

var j, k int = 7, 8

func main(){
	var i int
	var l, m = true, "momo"

	o, p := true, "dada"
	fmt.Println(i, j, k, l, m, o, p, c, python, cplus)
}
